# SOPS Configuration for Hill90
# Configure encryption rules for different environments

creation_rules:
  # Production secrets
  - path_regex: prod\.enc\.env$
    age: >-
      age1REPLACE_WITH_YOUR_AGE_PUBLIC_KEY_FROM_age-keygen

  # Development secrets
  - path_regex: dev\.enc\.env$
    age: >-
      age1REPLACE_WITH_YOUR_AGE_PUBLIC_KEY_FROM_age-keygen

# Note: After generating age keys with `age-keygen`,
# replace the placeholder above with your actual public key
