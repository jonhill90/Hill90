# Selectors in platform/observability/prometheus/alerts.yml that are allowed to
# match zero series on a healthy system, checked by check_alert_series.py.
#
# One line per selector, then '#' and the REASON. A selector with no reason is
# indistinguishable from a rule nobody has verified, which is the failure this
# whole check exists to catch. Keep this list short and argued.
#
# Format:  <metric-name-or-full-selector>    # why it can legitimately be absent


loki_request_duration_seconds_count{route="loki_api_v1_push", status_code!~"2.."}  # Lazily created per status code. THIS IS NOT THE LokiIngestionErrors BUG: status_code is a real label on this metric, carrying observed values 200, 204, 503 and "success" across routes, and a 503 has genuinely been recorded on route="ready". The ingest route has only ever returned 204 on this deployment, so the non-2xx series has not been created yet; it appears the moment a push is rejected. Contrast the rule this replaced, whose `status` label does not exist on its metric under any condition. Verified 2026-07-31.

hill90_e2e_delivery_test  # Exists only while an end-to-end delivery test is running. It is written by hand to /opt/hill90/metrics/textfile_collector/e2e-delivery-test.prom and deleted afterwards, so absent is the correct and expected steady state. If this series IS present outside a test, a previous test was not cleaned up and TEST_IGNORE_DeliveryPathCheck is firing.

hill90_scheduled_workflow_last_run_timestamp_seconds{workflow="vault-sync-to-sops"}  # TEMPORARY, NOT a legitimate steady state like the entries above — remove once resolved. Added 2026-08-05 during h#712's build: the other three workflow labels (audit-hill90-ui-client, deploy-drift, tenant-baseline-agreement) were confirmed live by dispatching each from the feature branch and querying the real production Prometheus — this one specifically was NOT dispatched, deliberately, because vault-sync-to-sops.yml's real run reads and can rewrite the SOPS store, and this session was under an explicit standing constraint (a separate, concurrent SOPS-exposure incident) not to touch anything secrets-adjacent. The rule's selector is syntactically identical to the other three (same metric name, same label shape) and the emit step in vault-sync-to-sops.yml is the same code, reviewed and bash -n/promtool-tested the same way — this entry exists only because it was not exercised tonight, not because anything about it is in doubt. Remove this line the first time either the natural Monday schedule or a deliberate dispatch writes hill90_scheduled_workflow_vault-sync-to-sops.prom for real, and confirm removal is safe by re-running check_alert_series.py afterward.
