name: Deploy (Staging Certificates)

# Staging certificates: unlimited rate limits, browser warnings expected
# Use this for testing and dev/stage branches
on:
  push:
    branches:
      - dev
      - stage
    paths:
      - 'src/**'
      - 'deployments/**'
      - 'scripts/deploy.sh'
      - 'scripts/build-and-deploy.sh'
      - '.github/workflows/deploy-staging.yml'

  workflow_dispatch:
    inputs:
      environment:
        description: 'Deployment environment'
        required: true
        type: choice
        options:
          - prod
        default: 'prod'

jobs:
  validate:
    name: Validate Infrastructure
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Validate Docker Compose files
        run: |
          echo "🔍 Validating Docker Compose configuration..."

          # Check if docker compose files exist
          if [[ ! -f deployments/compose/prod/docker-compose.yml ]]; then
            echo "❌ Missing deployments/compose/prod/docker-compose.yml"
            exit 1
          fi

          # Validate syntax (basic check)
          echo "✅ Docker Compose files found"

      - name: Validate deployment scripts
        run: |
          echo "🔍 Validating deployment scripts..."

          if [[ ! -f scripts/deploy.sh ]]; then
            echo "❌ Missing deploy.sh"
            exit 1
          fi

          if [[ ! -x scripts/deploy.sh ]]; then
            echo "❌ deploy.sh is not executable"
            exit 1
          fi

          echo "✅ Deployment scripts validated"

      - name: Validate secrets structure
        run: |
          echo "🔍 Validating encrypted secrets file..."

          if [[ ! -f infra/secrets/prod.enc.env ]]; then
            echo "❌ Missing prod.enc.env"
            exit 1
          fi

          echo "✅ Secrets file exists (encrypted)"

  deploy:
    name: Deploy Services (Staging Certs)
    runs-on: ubuntu-latest
    needs: validate
    environment: production

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Tailscale
        uses: tailscale/github-action@v2
        with:
          oauth-client-id: ${{ secrets.TAILSCALE_OAUTH_CLIENT_ID }}
          oauth-secret: ${{ secrets.TAILSCALE_OAUTH_SECRET }}
          tags: tag:ci

      - name: Setup SSH key
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.VPS_SSH_PRIVATE_KEY }}" > ~/.ssh/vps_key
          chmod 600 ~/.ssh/vps_key

      - name: Setup SOPS/age for secrets
        run: |
          # Install SOPS
          curl -L -o /tmp/sops https://github.com/getsops/sops/releases/download/v3.8.1/sops-v3.8.1.linux.amd64
          chmod +x /tmp/sops
          sudo mv /tmp/sops /usr/local/bin/sops

          # Setup age key
          mkdir -p ~/.config/sops/age
          echo "${{ secrets.SOPS_AGE_KEY }}" > ~/.config/sops/age/keys.txt
          chmod 600 ~/.config/sops/age/keys.txt

      - name: Get Tailscale IP from secrets
        id: get-ip
        run: |
          echo "🌐 Fetching VPS Tailscale IP from encrypted secrets..."
          TAILSCALE_IP=$(sops -d --extract '["TAILSCALE_IP"]' infra/secrets/prod.enc.env)
          echo "::add-mask::$TAILSCALE_IP"
          echo "tailscale_ip=$TAILSCALE_IP" >> $GITHUB_OUTPUT
          echo "Tailscale IP retrieved successfully"

      - name: Verify SSH connectivity
        run: |
          echo "🔌 Verifying SSH connectivity to VPS..."
          if ! ssh -i ~/.ssh/vps_key -o StrictHostKeyChecking=no -o ConnectTimeout=10 \
              deploy@${{ steps.get-ip.outputs.tailscale_ip }} 'echo "SSH connection successful"'; then
            echo "❌ Failed to connect to VPS via SSH"
            exit 1
          fi
          echo "✅ SSH connectivity verified"

      - name: Deploy to VPS with staging certificates
        run: |
          echo "📋 Using Let's Encrypt STAGING certificates"
          echo "   ⚠️  Certificates will show browser warnings"
          echo "   ✅ Unlimited rate limits (safe for testing)"
          echo ""

          # Deploy via SSH (defaults to staging certificates)
          ssh -i ~/.ssh/vps_key -o StrictHostKeyChecking=no \
            deploy@${{ steps.get-ip.outputs.tailscale_ip }} \
            "cd /opt/hill90/app && \
             git fetch origin && \
             git reset --hard origin/${{ github.ref_name }} && \
             export SOPS_AGE_KEY_FILE=/opt/hill90/secrets/keys/keys.txt && \
             bash scripts/deploy.sh prod"

      - name: Wait for services to start
        run: |
          echo "⏳ Waiting for services to start (60 seconds)..."
          sleep 60

      - name: Verify service health
        run: |
          echo "🏥 Verifying service health..."

          # Check Docker containers are running
          ssh -i ~/.ssh/vps_key -o StrictHostKeyChecking=no \
            deploy@${{ steps.get-ip.outputs.tailscale_ip }} \
            'docker ps --format "table {{.Names}}\t{{.Status}}" | grep -E "(api|traefik|auth|postgres)"'

          echo "✅ All services are running"

      - name: Summary
        if: success()
        run: |
          echo "🎉 Deployment complete!"
          echo ""
          echo "📊 Deployment details:"
          echo "  Environment: ${{ github.event.inputs.environment || 'prod' }}"
          echo "  Certificates: STAGING (browser warnings expected)"
          echo "  Branch: ${{ github.ref_name }}"
          echo "  Commit: ${{ github.sha }}"
          echo ""
          echo "⚠️  Note: Staging certificates will show browser warnings"
          echo "   This is expected and safe for testing"
          echo ""
          echo "✅ Next steps:"
          echo "  1. Verify services: make health"
          echo "  2. Check logs: make logs"
          echo "  3. Test endpoints (use -k flag for curl to skip cert validation)"

  health-check:
    name: Extended Health Check
    runs-on: ubuntu-latest
    needs: deploy
    if: success()

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Tailscale
        uses: tailscale/github-action@v2
        with:
          oauth-client-id: ${{ secrets.TAILSCALE_OAUTH_CLIENT_ID }}
          oauth-secret: ${{ secrets.TAILSCALE_OAUTH_SECRET }}
          tags: tag:ci

      - name: Setup SSH key
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.VPS_SSH_PRIVATE_KEY }}" > ~/.ssh/vps_key
          chmod 600 ~/.ssh/vps_key

      - name: Setup SOPS for secrets
        run: |
          curl -L -o /tmp/sops https://github.com/getsops/sops/releases/download/v3.8.1/sops-v3.8.1.linux.amd64
          chmod +x /tmp/sops
          sudo mv /tmp/sops /usr/local/bin/sops

          mkdir -p ~/.config/sops/age
          echo "${{ secrets.SOPS_AGE_KEY }}" > ~/.config/sops/age/keys.txt
          chmod 600 ~/.config/sops/age/keys.txt

      - name: Get Tailscale IP
        id: get-ip
        run: |
          TAILSCALE_IP=$(sops -d --extract '["TAILSCALE_IP"]' infra/secrets/prod.enc.env)
          echo "::add-mask::$TAILSCALE_IP"
          echo "tailscale_ip=$TAILSCALE_IP" >> $GITHUB_OUTPUT

      - name: Check container health
        run: |
          echo "🏥 Running extended health checks..."

          # Check all containers are running
          echo "📦 Checking container status..."
          ssh -i ~/.ssh/vps_key -o StrictHostKeyChecking=no \
            deploy@${{ steps.get-ip.outputs.tailscale_ip }} \
            'cd /opt/hill90/app && docker compose -f deployments/docker-compose.prod.yml ps'

      - name: Check container logs for errors
        run: |
          echo "📋 Checking recent logs for errors..."

          # Check for recent errors in logs (last 50 lines)
          ssh -i ~/.ssh/vps_key -o StrictHostKeyChecking=no \
            deploy@${{ steps.get-ip.outputs.tailscale_ip }} \
            'cd /opt/hill90/app && docker compose -f deployments/docker-compose.prod.yml logs --tail=50' \
            | grep -i "error\|fatal\|exception" || echo "No errors found in recent logs"

      - name: Verify Traefik dashboard
        run: |
          echo "🌐 Checking Traefik dashboard..."

          # Verify Traefik is responding
          ssh -i ~/.ssh/vps_key -o StrictHostKeyChecking=no \
            deploy@${{ steps.get-ip.outputs.tailscale_ip }} \
            'curl -f http://localhost:8080/api/http/routers || echo "⚠️  Traefik dashboard not responding"'

      - name: Health check summary
        run: |
          echo "✅ Extended health checks complete!"
          echo ""
          echo "All services appear to be healthy."
          echo "Monitor logs for the next few minutes to ensure stability."
